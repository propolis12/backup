{% extends 'base.html.twig' %}
{% block title %}Hello MainPageController!{% endblock %}
{% block head %}

{% endblock %}

{% block javascripts %}
    {{  parent() }}

    {# {{ encore_entry_script_tags('main') }}  #}
    {{ encore_entry_script_tags('mainPage') }}

    {% if is_granted("ROLE_USER") %}
        <script>
        window.username = '{{ app.user.username|e('js') }}';
        window.password = '{{ app.user.password|e('js') }}';
        </script>
    {%  endif %}
{% endblock %}

{% block stylesheets%}
    {# {{ encore_entry_link_tags('main') }} #}
    {{ encore_entry_link_tags('mainPage') }}
{% endblock %}
{% block body %}
    {{ parent() }}
    {{ include('forms/uploadForm.html.twig') }}

    
    <div class="row">
    {% for image in ownedImages %}
        <div class="col-2 px-0">
            <img class="card-img"  alt=" {{ image.filename }}"  src="{{ path('send_thumbnail', { filename: image.filename}) }}">
        </div>

    {% endfor %}

    </div>




       <div id="main"></div>


{% endblock %}


